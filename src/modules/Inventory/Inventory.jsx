import { Stack } from '@mui/material';

export const Inventory = () => {
    return (
        <Stack alignItems={'center'} justifyContent={'center'} py={10} px={4} width={'100%'}>
            Inventory
        </Stack>
    );
};
